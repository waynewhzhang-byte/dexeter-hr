{
  "name": "dexter-hr-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "db:migrate": "bun run infra/db/migrate.ts",
    "config-center:dev": "bun run apps/config-center/src/main.ts",
    "config-center:smoke": "env -u http_proxy -u https_proxy -u HTTP_PROXY -u HTTPS_PROXY -u ALL_PROXY bun run apps/config-center/scripts/smoke.ts",
    "release:pack": "env -u http_proxy -u https_proxy -u HTTP_PROXY -u HTTPS_PROXY -u ALL_PROXY bun run apps/config-center/scripts/release-pack.ts"
  },
  "dependencies": {
    "fastify": "^5.6.1",
    "pg": "^8.16.3",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@types/pg": "^8.15.5",
    "typescript": "^5.9.2"
  }
}
